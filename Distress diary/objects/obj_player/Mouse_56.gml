/// @description Insert description here
// You can write your code in this editor
if (!global.automatic_attack_enable && global.my_player_id == obj_id_player_only && global.attack_speed > 0)
{
	event_user(0);
}

